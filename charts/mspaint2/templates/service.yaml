apiVersion: v1
kind: Service
metadata:
  name: {{ template "mspaint2.fullname" . }}
  labels:
    app: {{ template "mspaint2.name" . }}
    chart: {{ template "mspaint2.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "mspaint2.name" . }}
    release: {{ .Release.Name }}
